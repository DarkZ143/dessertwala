import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Dessertwala  - Sweeten Your Day with Our Delicious Desserts",
  description: "Curated selection of delectable desserts, from classic favorites to innovative creations. Indulge in our irresistible treats and satisfy your sweet cravings with us. Experience the perfect blend of flavors and textures in every bite. Order now and treat yourself to a delightful dessert experience!",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
